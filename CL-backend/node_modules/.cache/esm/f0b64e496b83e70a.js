let Color;_6b8‍.x([["write",()=>write]]);_6b8‍.w("../../models/color",[["default",["Color"],function(v){Color=v}]]);

       const write = async (ctx) => {
  const { hex, R, G, B, likes } = ctx.request.body;
  const color = new Color({
    hex,
    R,
    G,
    B,
    likes,
  });
  try {
    await color.save();
    ctx.body = color;
  } catch (e) {
    ctx.throw(500, e);
  }
};
